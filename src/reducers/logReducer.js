const initialState = {
  logs: null,
  current: null,
  loading: false,
  error: null,
};

/* eslint-disable */
export default (state = initialState, action) => {
  switch (action.type) {
    default: {
      return state;
    }
  }
};
